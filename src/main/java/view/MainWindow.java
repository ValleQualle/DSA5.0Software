/*
package view;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;

public class MainWindow extends Application {

    Stage stage;

    @Override
    public void start(Stage primaryStage) {
        V_Character view = new V_Character();

        Scene scene = new Scene(view, 300, 200);
        primaryStage.setScene(scene);
        primaryStage.show();
    }

*/
/*    public characterView() {
        idInput.setPromptText("Character-ID eingeben");
        VBox vbox = new VBox(9);
        vbox.getChildren().addAll(idInput, fetchButton, nameLabel, ageLabel);
    }*//*




    public void viewCharacter(String nam, String spezi, int ag) {
        Label lab_name = new Label(nam);
        Label lab_spezies = new Label(spezi);
        Label lab_age = new Label(ag + "");

        VBox vbox = new VBox(10); // Die angegebene Zahl gibt den vertikalen Abstand zwischen den Elementen an
        vbox.getChildren().addAll(lab_name, lab_spezies, lab_age); // Die label werden in der VBox angeordnet

        Scene scene = new Scene(vbox, 300, 200);

        stage.setScene(scene);
        stage.show();
    }
}
*/
